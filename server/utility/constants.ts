// Errors
export const JWT_EXPIRED_ERROR = `JWT Token Expired or Decode Error. Login again to continue.`;
export const INSUFFICIENT_ROLES = `It seems like you do not have sufficient roles to access the dashboard.`;
export const JWT_DECODE_AT_PROXY = `JWT Decode/Timeout Error Recorded while accessing`;

export const JWT_EXPIRE_TIME = '10h';
export const BEARER_TOKEN = 'bearer ';
