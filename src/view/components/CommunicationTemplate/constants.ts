export enum detailHeaderType {
  content = 'content',
  message = 'message',
  eventText = 'event_text',
}

export enum payloadPreviewLabelType {
  preview = 'Preview',
  payloadNPreview = 'Payload(s) & Preview',
  payloads = 'Payload(s)',
}

export enum previewHeaderClassNames {
  class1 = 'bg-white break-words px-2 py-2 mx-auto max-w-fit min-w-full',
  class2 = 'bg-white break-words px-2 py-2 mx-auto max-w-fit',
}

export enum previewClassNames {
  class1 = 'min-h-[98px] max-h-full min-w-[298px] text-left mx-auto px-2 py-2',
  class2 = 'min-h-[98px] max-h-full min-w-[298px] text-left mx-auto px-2 py-2 whitespace-pre-wrap',
}
