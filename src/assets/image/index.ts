const logo = require('./logo.svg') as string;
const refreshIcon = require('./refresh.png') as string;
const emptyState = require('./emptyState.svg') as string;

export { logo, refreshIcon, emptyState };
